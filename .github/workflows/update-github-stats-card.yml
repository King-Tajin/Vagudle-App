name: Update GitHub Stats Card

on:
  push:
    branches:
      - master
  schedule:
    - cron: "0 6 * * 2"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-card:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Switch to stats branch
        run: |
          git fetch origin stats || true
          if git show-ref --verify --quiet refs/remotes/origin/stats; then
            git checkout stats
          else
            git checkout --orphan stats
            git rm -rf .
          fi

      - name: Fetch repo stats
        id: stats
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          repo_json=$(curl -s -H "Authorization: Bearer $GH_TOKEN" https://api.github.com/repos/King-Tajin/Vagudle-App)
          sub_json=$(curl -s -H "Authorization: Bearer $GH_TOKEN" https://api.github.com/repos/King-Tajin/Vagudle)

          stars=$(echo "$repo_json" | jq -r '.stargazers_count')
          forks=$(echo "$repo_json" | jq -r '.forks_count')
          issues=$(echo "$repo_json" | jq -r '.open_issues_count')
          watchers=$(echo "$repo_json" | jq -r '.subscribers_count')

          commits=$(curl -s -I -H "Authorization: Bearer $GH_TOKEN" "https://api.github.com/repos/King-Tajin/Vagudle-App/commits?per_page=1" | grep -i '^link:' | grep -oP 'page=\K[0-9]+(?=>; rel="last")')
          if [ -z "$commits" ]; then commits=1; fi

          size_kb_app=$(echo "$repo_json" | jq -r '.size')
          size_kb_sub=$(echo "$sub_json" | jq -r '.size')
          size_kb=$(( size_kb_app + size_kb_sub ))
          if [ "$size_kb" -ge 1024 ] 2>/dev/null; then
            repo_size=$(awk "BEGIN { printf \"%.1f MB\", $size_kb/1024 }")
          else
            repo_size="${size_kb} KB"
          fi

          commit_json=$(curl -s -H "Authorization: Bearer $GH_TOKEN" "https://api.github.com/repos/King-Tajin/Vagudle-App/commits?per_page=1")
          commit_sha=$(echo "$commit_json" | jq -r '.[0].sha[0:7]')
          commit_date=$(echo "$commit_json" | jq -r '.[0].commit.author.date' | cut -d'T' -f1)
          commit_message_raw=$(echo "$commit_json" | jq -r '.[0].commit.message' | head -n1)
          commit_message_escaped=$(printf '%s' "$commit_message_raw" | sed 's/&/\&amp;/g; s/</\&lt;/g; s/>/\&gt;/g')
          msg_wrapped=$(printf '%s' "$commit_message_escaped" | fold -s -w 52)
          msg_line1=$(echo "$msg_wrapped" | sed -n '1p')
          msg_line2=$(echo "$msg_wrapped" | sed -n '2p')
          msg_line3=$(echo "$msg_wrapped" | sed -n '3p')
          msg_line4=$(echo "$msg_wrapped" | sed -n '4p')
          msg_line5=$(echo "$msg_wrapped" | sed -n '5p')
          msg_total_lines=$(echo "$msg_wrapped" | wc -l)
          if [ "$msg_total_lines" -lt 1 ]; then msg_total_lines=1; fi
          if [ "$msg_total_lines" -gt 5 ]; then
            msg_line5="${msg_line5%???}..."
            msg_lines=5
          else
            msg_lines=$msg_total_lines
          fi

          languages_json_app=$(curl -s -H "Authorization: Bearer $GH_TOKEN" https://api.github.com/repos/King-Tajin/Vagudle-App/languages)
          languages_json_sub=$(curl -s -H "Authorization: Bearer $GH_TOKEN" https://api.github.com/repos/King-Tajin/Vagudle/languages)
          languages_json=$(jq -n --argjson a "$languages_json_app" --argjson b "$languages_json_sub" '(($a | keys) + ($b | keys) | unique) as $keys | reduce $keys[] as $k ({}; .[$k] = (($a[$k] // 0) + ($b[$k] // 0)))')
          language=$(echo "$languages_json" | jq -r 'to_entries | sort_by(-.value) | .[0].key')
          lang_breakdown=$(echo "$languages_json" | jq -r 'to_entries | sort_by(-.value) | (map(.value) | add) as $total | .[0:5] | map("\(.key) \((.value * 100 / $total) | floor)%") | join(", ")')
          lang_wrapped=$(printf '%s' "Languages: $lang_breakdown" | fold -s -w 52)
          lang_line1=$(echo "$lang_wrapped" | sed -n '1p')
          lang_line2=$(echo "$lang_wrapped" | sed -n '2p')
          lang_line3=$(echo "$lang_wrapped" | sed -n '3p')
          lang_total_lines=$(echo "$lang_wrapped" | wc -l)
          if [ "$lang_total_lines" -gt 3 ]; then
            lang_line3="${lang_line3%???}..."
          fi
          if [ "$lang_total_lines" -ge 3 ]; then
            lang_lines=3
          elif [ "$lang_total_lines" -eq 2 ]; then
            lang_lines=2
          else
            lang_lines=1
          fi

          echo "stars=$stars" >> "$GITHUB_OUTPUT"
          echo "forks=$forks" >> "$GITHUB_OUTPUT"
          echo "issues=$issues" >> "$GITHUB_OUTPUT"
          echo "watchers=$watchers" >> "$GITHUB_OUTPUT"
          echo "language=$language" >> "$GITHUB_OUTPUT"
          echo "commits=$commits" >> "$GITHUB_OUTPUT"
          echo "repo_size=$repo_size" >> "$GITHUB_OUTPUT"
          echo "commit_sha=$commit_sha" >> "$GITHUB_OUTPUT"
          echo "commit_date=$commit_date" >> "$GITHUB_OUTPUT"
          echo "msg_line1=$msg_line1" >> "$GITHUB_OUTPUT"
          echo "msg_line2=$msg_line2" >> "$GITHUB_OUTPUT"
          echo "msg_line3=$msg_line3" >> "$GITHUB_OUTPUT"
          echo "msg_line4=$msg_line4" >> "$GITHUB_OUTPUT"
          echo "msg_line5=$msg_line5" >> "$GITHUB_OUTPUT"
          echo "msg_lines=$msg_lines" >> "$GITHUB_OUTPUT"
          echo "lang_line1=$lang_line1" >> "$GITHUB_OUTPUT"
          echo "lang_line2=$lang_line2" >> "$GITHUB_OUTPUT"
          echo "lang_line3=$lang_line3" >> "$GITHUB_OUTPUT"
          echo "lang_lines=$lang_lines" >> "$GITHUB_OUTPUT"

      - name: Generate stats card
        run: |
          mkdir -p stats

          msg_lines="${{ steps.stats.outputs.msg_lines }}"
          extra_height=$(( (msg_lines - 1) * 14 ))

          msg_y1=257
          msg_y2=$((msg_y1 + 14))
          msg_y3=$((msg_y2 + 14))
          msg_y4=$((msg_y3 + 14))
          msg_y5=$((msg_y4 + 14))
          sep_y=$((msg_y1 + 13 + extra_height))
          lang1_y=$((sep_y + 20))

          lang_lines="${{ steps.stats.outputs.lang_lines }}"
          langB1_y=$((lang1_y + 18))
          langB2_y=$((langB1_y + 18))
          langB3_y=$((langB2_y + 18))
          total_height=$((lang1_y + lang_lines * 18 + 14))
          rect_height=$((total_height - 4))

          line2_svg=""
          line3_svg=""
          line4_svg=""
          line5_svg=""
          if [ "$msg_lines" -ge 2 ]; then
            line2_svg='<text x="20" y="'"$msg_y2"'" font-family="monospace" font-size="11" fill="#6b7280">${{ steps.stats.outputs.msg_line2 }}</text>'
          fi
          if [ "$msg_lines" -ge 3 ]; then
            line3_svg='<text x="20" y="'"$msg_y3"'" font-family="monospace" font-size="11" fill="#6b7280">${{ steps.stats.outputs.msg_line3 }}</text>'
          fi
          if [ "$msg_lines" -ge 4 ]; then
            line4_svg='<text x="20" y="'"$msg_y4"'" font-family="monospace" font-size="11" fill="#6b7280">${{ steps.stats.outputs.msg_line4 }}</text>'
          fi
          if [ "$msg_lines" -ge 5 ]; then
            line5_svg='<text x="20" y="'"$msg_y5"'" font-family="monospace" font-size="11" fill="#6b7280">${{ steps.stats.outputs.msg_line5 }}</text>'
          fi

          lang_line1_svg='<text x="20" y="'"$langB1_y"'" font-family="monospace" font-size="11" fill="#9ca3af">${{ steps.stats.outputs.lang_line1 }}</text>'
          lang_line2_svg=""
          lang_line3_svg=""
          if [ "$lang_lines" -ge 2 ]; then
            lang_line2_svg='<text x="20" y="'"$langB2_y"'" font-family="monospace" font-size="11" fill="#9ca3af">${{ steps.stats.outputs.lang_line2 }}</text>'
          fi
          if [ "$lang_lines" -ge 3 ]; then
            lang_line3_svg='<text x="20" y="'"$langB3_y"'" font-family="monospace" font-size="11" fill="#9ca3af">${{ steps.stats.outputs.lang_line3 }}</text>'
          fi

          cat > stats/vagudle-stats.svg << SVG_EOF
          <svg width="380" height="$total_height" viewBox="0 0 380 $total_height" xmlns="http://www.w3.org/2000/svg">
            <rect x="2" y="2" width="376" height="$rect_height" rx="8" fill="#000000" stroke="#FFD700" stroke-width="4"/>
            <text x="20" y="34" font-family="monospace" font-size="17" font-weight="bold" fill="#FFD700">Vagudle-App</text>
            <text x="20" y="53" font-family="monospace" font-size="11" fill="#9ca3af">GitHub Repository Stats</text>
            <line x1="20" y1="64" x2="360" y2="64" stroke="#2a2a2a" stroke-width="1"/>

            <text x="20" y="90" font-family="monospace" font-size="13" fill="#ffffff">Commits</text>
            <text x="360" y="90" font-family="monospace" font-size="13" fill="#FFD700" text-anchor="end">${{ steps.stats.outputs.commits }}</text>

            <text x="20" y="115" font-family="monospace" font-size="13" fill="#ffffff">Stars</text>
            <text x="360" y="115" font-family="monospace" font-size="13" fill="#FFD700" text-anchor="end">${{ steps.stats.outputs.stars }}</text>

            <text x="20" y="140" font-family="monospace" font-size="13" fill="#ffffff">Forks</text>
            <text x="360" y="140" font-family="monospace" font-size="13" fill="#FFD700" text-anchor="end">${{ steps.stats.outputs.forks }}</text>

            <text x="20" y="165" font-family="monospace" font-size="13" fill="#ffffff">Open Issues</text>
            <text x="360" y="165" font-family="monospace" font-size="13" fill="#FFD700" text-anchor="end">${{ steps.stats.outputs.issues }}</text>

            <text x="20" y="190" font-family="monospace" font-size="13" fill="#ffffff">Watchers</text>
            <text x="360" y="190" font-family="monospace" font-size="13" fill="#FFD700" text-anchor="end">${{ steps.stats.outputs.watchers }}</text>

            <text x="20" y="215" font-family="monospace" font-size="13" fill="#ffffff">Repo Size</text>
            <text x="360" y="215" font-family="monospace" font-size="13" fill="#FFD700" text-anchor="end">${{ steps.stats.outputs.repo_size }}</text>

            <text x="20" y="240" font-family="monospace" font-size="13" fill="#ffffff">Latest Commit</text>
            <text x="360" y="240" font-family="monospace" font-size="13" fill="#FFD700" text-anchor="end">${{ steps.stats.outputs.commit_sha }} (${{ steps.stats.outputs.commit_date }})</text>
            <text x="20" y="$msg_y1" font-family="monospace" font-size="11" fill="#6b7280">${{ steps.stats.outputs.msg_line1 }}</text>
            $line2_svg
            $line3_svg
            $line4_svg
            $line5_svg

            <line x1="20" y1="$sep_y" x2="360" y2="$sep_y" stroke="#2a2a2a" stroke-width="1"/>

            <text x="20" y="$lang1_y" font-family="monospace" font-size="11" fill="#9ca3af">Primary language: ${{ steps.stats.outputs.language }}</text>
            $lang_line1_svg
            $lang_line2_svg
            $lang_line3_svg
          </svg>
          SVG_EOF

      - name: Commit card
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add stats/vagudle-stats.svg
          git diff --staged --quiet || git commit -m "Update GitHub stats card"
          git push origin HEAD:stats